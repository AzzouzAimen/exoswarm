# Data boundary

`cached/` is reserved for real cached TESS light curves and Target Pixel Files. `targets/` contains
agent-safe opaque manifests. Real identity mappings and catalog truth belong under `ground_truth/`
and remain backend-only. No scientific fixture in this scaffold contains fabricated measurements.

